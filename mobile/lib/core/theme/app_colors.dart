import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Sesuai HTML Stitch: #2b6cee)
  static const Color primary = Color(0xFF2B6CEE); 
  static const Color primaryDark = Color(0xFF1A4DBE); // Estimasi warna hover/darker
  
  // Text Colors
  static const Color textPrimary = Color(0xFF0D121B); // Sesuai HTML
  static const Color textSecondary = Color(0xFF4C669A); // Sesuai HTML
  static const Color textLight = Colors.white;

  // Backgrounds
  static const Color background = Colors.white; 
  static const Color surface = Color(0xFFF6F6F8);
  static const Color inputBackground = Colors.white; // atau F8F9FC sesuai referensi 2
  
  // Borders
  static const Color border = Color(0xFFCFD7E7);
}